# UC metadata skeleton — gold base tables.
# Generated by scripts/_gen_metadata_skeletons.py — populate comments in backfill phase.
tables:
  - name: gold_spc_quality_metric_subgroup
    comment: "SPC subgroup MV — sample-grain rows from the quality result-grain silver (QASR per-sample means), enriched with lot context (QALS), spec limits / MIC metadata (QAMV), and material/plant names. Governed replacement for the legacy connected_plant_uat.gold.spc_quality_metric_subgroup_mv (decommissioned). Adapter-facing view: spc_quality_metric_subgroup_mv (see spc_serving_views_*.sql). Grain: one row per QASR sample (lot × operation × mic × sample_number). Subgroup key: plant_id × material_id × mic_id × operation_id × batch_id."
    tags:
      layer: gold
      product_area: spc
    columns:
      - {name: plant_code, comment: ""}

